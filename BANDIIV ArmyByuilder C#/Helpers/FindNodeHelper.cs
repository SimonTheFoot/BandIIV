namespace BandiIVWebApiGetter.Helpers
{
    public static class FindNodeHelper
    {
        //    public static IEnumerable<TreeNode>FindNode(this TreeNode root)
        //    {
        //        yield return root;
        //        foreach (TreeNode node in root.Nodes)
        //        foreach (var item in FindNode(node))
        //    yield return item;
        //    }

        //    public static IEnumerable<TreeNode> FindFromTreeview(this TreeView tv)
        //    {
        //        foreach (TreeNode aNode in tv.Nodes)
        //        {
        //            yield return aNode;
        //            foreach (TreeNode node in aNode.Nodes)
        //            foreach (var item in FindNode(node))
        //                yield return item;
        //        }
        //    }
        //}
    }
}
